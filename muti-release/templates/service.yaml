apiVersion: v1
kind: Service
metadata:
  name: {{ include "muti-release.fullname" . }}
  labels:
    {{- include "muti-release.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "muti-release.selectorLabels" . | nindent 4 }}
